const del = $("#delete");

del.on("click", () => {
    //hidden html -> to appear
    //has button to complete
    // prevent to avoid the transition from the form to the POST-req
    // better to have 2 POST-req - one for delete acc and one for rest
});
